# CAI-Vision — all-in-one requirements (CUDA 13.0 + everything else)
#
# This is the working setup used on the training desktop. If you only need
# to run inference / export / evaluate, use `requirements-stable.txt`
# (CPU-only, no nightly torch). If you want to reproduce the training env
# on another machine, use `requirements-cuda130.txt` plus the stable deps.
#
# Quick reference:
#   pip install -r requirements.txt            # <- this file (CUDA 13.0)
#   pip install -r requirements-stable.txt     # <- CPU only, inference/eval
#   pip install -r requirements-cuda130.txt    # <- nightly torch/torchvision only

# --- Core training (CUDA 13.0 builds) ---
--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.10.0
torchvision==0.25.0
# torchaudio is optional for this project; uncomment if needed
# torchaudio==2.4.0+cu121

# --- Other deps (CPU-agnostic) ---
timm==0.9.12
numpy==2.3.3
pillow==10.0.0
tqdm==4.66.0
tensorboard==2.14.0
onnx==1.18.0
onnxruntime==1.22.1
onnxscript==0.5.2
# Optional:
# onnxruntime-tools>=1.8